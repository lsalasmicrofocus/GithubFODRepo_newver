name: Fortify on Demand New

on: 
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  FoD-SAST-Scan:
    runs-on: ubuntu-latest
    
    steps:    
      - name: Check out source code
        uses: actions/checkout@v4  

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin' # See 'Supported distributions' for available options
          java-version: '17'

      - name: Run FoD SAST Scan
        uses: fortify/github-action@v1
        with:
          sast-scan: true
        env:
          FOD_URL: https://ams.fortify.com
          FOD_TENANT: ${{secrets.FOD_TENANT}}
          FOD_CLIENT_ID: ${{secrets.FOD_CLIENT_ID}}
          FOD_CLIENT_SECRET: ${{secrets.FOD_CLIENT_SECRET}}
          FOD_RELEASE: ${{secrets.FOD_RELEASE_ID}}
          EXTRA_PACKAGE_OPTS: -bt none 
          DO_WAIT: true
          DO_EXPORT: true

      - name: Set up ScanCentral log directory
        run: |
          mkdir -p ${{ github.workspace }}/logs
          # Simulate ScanCentral log generation (replace with the actual ScanCentral command)
          echo "ScanCentral logs for the project" > ${{ github.workspace }}/logs/scancentral.log
          # Replace the above line with the actual ScanCentral scan command
          # For example, scancentral scan --output ${{ github.workspace }}/logs/scancentral.log

          # If you have a package to upload (e.g., .zip), you can add this step to generate the package
          zip -r ${{ github.workspace }}/scancentral-package.zip ${{ github.workspace }}/logs

      - name: Upload ScanCentral Logs as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: scancentral-logs
          path: ${{ github.workspace }}/logs/

      - name: Upload ScanCentral Package as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: scancentral-package
          path: ${{ github.workspace }}/scancentral-package.zip
